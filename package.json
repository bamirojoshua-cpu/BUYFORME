{
  "name": "buyforme",
  "private": true,
  "type": "module",
  "scripts": {
    "config": "node scripts/generate-runtime-config.js",
    "check": "node scripts/check-syntax.js",
    "pwa:tags": "node scripts/ensure-pwa-tags.js",
    "dev": "node scripts/generate-runtime-config.js && node scripts/ensure-pwa-tags.js && node scripts/check-syntax.js && vite",
    "build": "node scripts/generate-runtime-config.js && node scripts/ensure-pwa-tags.js && node scripts/check-syntax.js && vite build",
    "build:pages": "npm run build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.3",
    "vite-plugin-pwa": "^1.3.0"
  }
}
